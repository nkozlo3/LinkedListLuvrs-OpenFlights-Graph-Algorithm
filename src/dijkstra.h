#pragma once

#include <iostream>
#include "graph.h"
class Dijkstras
{
public:
    Dijkstras();      // TODO:
    Graph getGraph(); // TODO:

    // new functions
    int minDist(std::vector<int> distances, std::vector<bool> visited);

private:
};